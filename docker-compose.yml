networks:
  # Shared network for llama-server and MCP server to communicate.
  # Note: NOT internal — cloudflared needs internet access to connect to Cloudflare.
  llama-net:

services:
  # ---------------------------------------------------------------------------
  # Model preparation (download / quantize / convert) — run before the server.
  # All model prep is handled here; the runtime image only serves /models.
  #
  # Build once:
  #   docker compose build llama-convert
  #
  # Then prepare a model (output lands in ./models via the shared volume):
  #
  #   Pre-built GGUF on HuggingFace:
  #     docker compose run --rm llama-convert download qwen3.5-27b --quant Q4_K_M
  #
  #   No pre-built quant but fp16 GGUF exists (quantized locally):
  #     docker compose run --rm llama-convert download qwopus3.6-27b --quant TQ2_0
  #
  #   Safetensors-only repo (convert → fp16 GGUF → quantize):
  #     docker compose run --rm llama-convert convert-st qwen3.6-35b-a3b --quant TQ2_0
  #
  #   Re-quantize an existing GGUF already in ./models:
  #     docker compose run --rm llama-convert convert /models/model-Q8_0.gguf --quant Q4_K_M
  #
  # After preparation, start the server as normal:
  #   docker compose up llama-server
  # ---------------------------------------------------------------------------
  llama-convert:
    build:
      context: .
      dockerfile: Dockerfile
      target: convert
      args:
        CUDA_ARCHS: ${CUDA_ARCHS:-86}
    image: llama-convert:latest
    container_name: llama-convert
    profiles: ["convert"]

    volumes:
      - ./models:/models:rw
      - ./config:/config:rw
      - ./calibration-data:/calibration-data:ro

    environment:
      HF_TOKEN: ${HF_TOKEN:-}

      # Throttle download write speed to prevent WSL2 vmmem balloon / BSOD.
      # Uses aria2c --max-download-limit when set (e.g. CONVERT_DOWNLOAD_RATE=300M).
      # Leave unset to download at full speed.
      CONVERT_DOWNLOAD_RATE: ${CONVERT_DOWNLOAD_RATE:-}

      # Limit CPU threads used by llama-quantize during local quantization.
      # Leaving this unset defaults to cpu_count//2, which reserves half the
      # logical cores for the Windows host and prevents CLOCK_WATCHDOG_TIMEOUT
      # BSODs caused by Docker saturating every CPU core.
      # Override in .env: CONVERT_THREADS=4
      CONVERT_THREADS: ${CONVERT_THREADS:-}

      # Plain-text corpus for TriAttention calibration (per-head RoPE frequency stats).
      # Wikitext-2-raw-test (~313k tokens) is the default; override in .env if needed.
      # Set to empty string to skip calibration entirely.
      TRIATTENTION_INPUT: ${TRIATTENTION_INPUT:-/calibration-data/wikitext-2-raw-test.txt}

      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility

    gpus: all

  # ---------------------------------------------------------------------------
  # llama.cpp inference server (TurboQuant fork)
  # Exposed publicly via Cloudflare Tunnel at chat.jessfan.com with
  # Cloudflare Access authentication.
  # ---------------------------------------------------------------------------
  llama-server:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
      args:
        CUDA_ARCHS: ${CUDA_ARCHS:-86}
    image: llama-server:latest
    container_name: llama-server
    deploy:
      resources:
        limits:
          cpus: "16.0"
          memory: 14G
        reservations:
          cpus: "8"
          memory: 4G

    # No host port mapping — accessible only via Cloudflare Tunnel.

    volumes:
      - ./models:/models:rw
      # Mount entire config dir so models.ini, webui-config.json, and
      # triattention .bin stats files are all accessible at /etc/llama-server/.
      - ./config:/etc/llama-server:ro

    # Allow time for slot KV-cache saves to complete on SIGTERM before SIGKILL.
    # slot-flush-interval=120s means the worst-case un-flushed window is 2 min,
    # but the shutdown save itself (one round of POST /slots/?action=save per
    # loaded model) can take 10-60 s depending on KV size.
    # Rule of thumb: set to max(slot-flush-interval, expected_save_time) + 10s.
    stop_grace_period: 180s

    gpus: all

    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility

      # --- Entrypoint vars (see entrypoint.sh) ---
      LLAMA_HOST: ${LLAMA_HOST:-0.0.0.0}
      LLAMA_PORT: ${LLAMA_PORT:-8080}
      LLAMA_API_KEY: ${LLAMA_API_KEY:-}
      MCP_API_KEY: ${MCP_API_KEY:-}
      CF_ACCESS_CLIENT_ID: ${CF_ACCESS_CLIENT_ID:-}
      CF_ACCESS_CLIENT_SECRET: ${CF_ACCESS_CLIENT_SECRET:-}
      LLAMA_MODELS_MAX: ${LLAMA_MODELS_MAX:-1}
      LLAMA_MODELS_AUTOLOAD: ${LLAMA_MODELS_AUTOLOAD:-on}

      # --- CUDA tuning (env vars read directly by ggml-cuda) ---
      # NOTE: two DIFFERENT graph knobs exist, do not confuse them:
      #   GGML_CUDA_DISABLE_GRAPHS — presence-only (getenv()!=nullptr). Setting it
      #     to ANY value (even "0") turns CUDA graph capture/replay OFF entirely.
      #     This is the real on/off switch (see below).
      #   GGML_CUDA_GRAPH_OPT — atoi(env)==1 enables an extra multi-stream
      #     concurrency OPTIMIZATION pass on top of graphs (forks the captured
      #     graph across secondary CUDA streams). It does NOT enable/disable graph
      #     replay; it is a no-op when graphs are already disabled. MUST be 0 for
      #     this deployment: the multi-stream replay races with the out-of-band
      #     checkpoint copy and causes the 160k illegal-memory-access crash. The
      #     override sets it to "0"; graphs run single-stream and stay stable.
      # NOTE: main file defaults to 1, but override (docker-compose.override.yml) sets to 0.
      # See override comments for rationale (multi-stream graph replay race).
      GGML_CUDA_GRAPH_OPT: ${GGML_CUDA_GRAPH_OPT:-1}
      CUDA_SCALE_LAUNCH_QUEUES: ${CUDA_SCALE_LAUNCH_QUEUES:-2x}

      # --- CUDA graphs on/off (qwopus 160k crash mitigation) ---
      #
      # Long-context spec-decode mutates the KV cache out-of-band (TriAttention
      # physical compaction AND on-device checkpoint writes) while a CUDA graph is
      # replayed, producing a deferred illegal memory access. BOTH triggers are
      # now mitigated, so graphs are kept ENABLED:
      #   - compaction: fixed in-source (ggml_backend_cuda_graph_clear after each
      #     compaction, 686e3d44c).
      #   - checkpoint write: caused by the multi-stream graph concurrency pass
      #     racing the out-of-band copy; mitigated by GGML_CUDA_GRAPH_OPT=0
      #     (single-stream graphs) set in the override. VALIDATED at 160k.
      #
      # FOOTGUN: this knob is presence-only, and the map form always emits the
      # key, so a host value can NOT re-enable graphs (even =0 keeps them OFF).
      # It is left COMMENTED so graphs stay ON. To force graphs OFF (fallback),
      # uncomment the line below (in this file or the override).
      # GGML_CUDA_DISABLE_GRAPHS: ${GGML_CUDA_DISABLE_GRAPHS:-1}

      # --- Crash localisation ---
      #
      # IMPORTANT: CUDA graph replay bypasses individual kernel launch sites, so
      # per-kernel sync checks (GGML_CUDA_SYNC_DEBUG) and CUDA_LAUNCH_BLOCKING will
      # NOT correctly attribute the failing kernel unless CUDA graphs are disabled.
      # When debugging, ensure GGML_CUDA_DISABLE_GRAPHS is set (it is, above).
      #
      # Option A: CUDA_LAUNCH_BLOCKING (no rebuild needed, ~3-10x slower)
      #   Uncomment below (graphs must already be disabled via the knob above):
      #   CUDA_LAUNCH_BLOCKING: 1
      CUDA_LAUNCH_BLOCKING: ${CUDA_LAUNCH_BLOCKING:-0}
      #
      # Option B: GGML_CUDA_SYNC_DEBUG build flag (rebuild with CUDA_SYNC_DEBUG=1)
      #   Checks are compiled in unconditionally — always active in this image.
      #   Also requires graphs disabled (GGML_CUDA_DISABLE_GRAPHS, above) for
      #   accurate attribution.
      # GGML_CUDA_SYNC_DEBUG is a compile-time flag — no env var needed

      # All other configuration (ctx, KV cache, sampling, speculative decoding,
      # DRY, reasoning budget, etc.) lives in scripts/models.ini.

    networks:
      - llama-net
    restart: unless-stopped

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${LLAMA_PORT:-8080}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 300s  # allow up to 5 min for large model to load from disk

  # ---------------------------------------------------------------------------
  # Cloudflare Tunnel — exposes both llama-server and mcp-search-server publicly:
  #   - chat.jessfan.com → http://llama-server:8080
  #   - mcp.jessfan.com → http://mcp-search-server:3100
  # Both use Cloudflare Access authentication. Makes an outbound connection to Cloudflare.
  #
  # One-time setup in Cloudflare Zero Trust → Networks → Tunnels:
  #   1. Create tunnel → Docker → copy token to CF_TUNNEL_TOKEN in .env
  #   2. Add Public Hostname: chat.jessfan.com → http://llama-server:8080
  #   3. Add Public Hostname: mcp.jessfan.com → http://mcp-search-server:3100
  #   (Cloudflare creates the CNAME DNS records automatically.)
  #
  # Cloudflare Access setup (Zero Trust → Access → Applications):
  #   1. Create Application for chat.jessfan.com
  #   2. Create Application for mcp.jessfan.com
  #   3. Choose authentication method for each:
  #      - Google OAuth (same credentials as chat.jessfan.com)
  #      - Email/Password (users get a one-time code via email)
  #   4. Set "Who can access" to your email or "Anyone with the domain"
  # ---------------------------------------------------------------------------
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: "${CF_TUNNEL_TOKEN:-}"
    networks:
      - llama-net
    restart: unless-stopped
    depends_on:
      - llama-server
      - mcp-search-server
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.5"
          memory: 256M

  # ---------------------------------------------------------------------------
  # MCP Search Server — semantic web search with browser automation
  # Provides MCP tools accessible on the internal llama-net network
  # ---------------------------------------------------------------------------
  mcp-search-server:
    build:
      context: ./mcp-search-server
      dockerfile: Dockerfile
    container_name: mcp-search-server

    ports:
      - "${MCP_SERVER_HOST:-127.0.0.1}:${MCP_SERVER_PORT:-3100}:3100"

    volumes:
      # mcp-files: persistent output dir for screenshots and generated files.
      # Named volume ensures appuser ownership is preserved across restarts.
      - mcp-files:/app/mcp-files

    environment:
      MCP_SERVER_HOST: ${MCP_SERVER_HOST:-0.0.0.0}
      MCP_SERVER_PORT: ${MCP_SERVER_PORT:-3100}
      FILE_BASE_URL: ${FILE_BASE_URL:-https://mcp.jessfan.com}
      SEARCH_ENGINE: ${SEARCH_ENGINE:-duckduckgo}
      SEARCH_API_KEY: ${SEARCH_API_KEY:-}
      BROWSER_TIMEOUT: ${BROWSER_TIMEOUT:-30}
      MAX_RESULTS: ${MAX_RESULTS:-10}
      CACHE_ENABLED: ${CACHE_ENABLED:-true}
      CACHE_TTL: ${CACHE_TTL:-3600}
      MCP_API_KEY: ${MCP_API_KEY:-}
      MCP_CORS_ORIGINS: ${MCP_CORS_ORIGINS:-*}
      ADVISOR_BASE_URL: ${ADVISOR_BASE_URL:-http://localhost:8080/v1}
      ADVISOR_MODEL: ${ADVISOR_MODEL:-qwopus3.6-27b}
      ADVISOR_API_KEY: ${ADVISOR_API_KEY:-}
      ADVISOR_MAX_TOKENS: ${ADVISOR_MAX_TOKENS:-8192}
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "0.5"
          memory: 512M

    restart: unless-stopped

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${MCP_SERVER_PORT:-3100}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

    networks:
      - llama-net


volumes:
  mcp-files:
    driver: local